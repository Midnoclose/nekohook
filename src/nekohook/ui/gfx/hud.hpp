
#pragma once

namespace nekohook::hud {

void Reset();
void Draw(std::string_view);
// TODO, console log below Draw(str), fade over time. use as an easy look into debug info when not compiled release

}
